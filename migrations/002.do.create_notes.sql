CREATE TABLE noteful_notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    modified TIMESTAMP DEFAULT now() NOT NULL,
    folderId INTEGER REFERENCES noteful_folders ON DELETE CASCADE NOT NULL,
    content TEXT
);